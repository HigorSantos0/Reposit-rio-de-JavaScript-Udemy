// string, number, undefined, boolean e symbol(Só sera falado mais pra frente)

const nome = 'Higor';
const nome1 = "Pamela";
const nome2 = `Deise`;
const num = 10;
const num2 = 10.5;

let nomeAluno; //undefined = não aponta pra lugar nenhum na memória
const sobreNomeAluno = null; // nulo -> Não aponta pra local nenhum na memória
const aprovado = true; //boolean = false ou true
